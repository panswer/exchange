export enum MessageActionEnum {
  SUPPRESS = "SUPPRESS",
}

export enum UserAttributesEnum {
  EMAIL = "email",
  EMAIL_VERIFIED = "email_verified",
}

export enum AdminInitiateAuthEnum {
  ADMIN_USER_PASSWORD_AUTH = "ADMIN_USER_PASSWORD_AUTH",
}
